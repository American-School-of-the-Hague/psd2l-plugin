<queries>
	<!--set name here (also applies to permissions_root-->
    <query name="com.txoof.brightspace.students.usernames" coreTable="students" flattened="false">
		<!--add description here-->
        <description>export all active students grades 5-13 for use with a BrightSpace 7-User import</description>
		<!--number of columns here must match number sql returns-->
        <columns>
			<column column="students.student_number">id</column>
			<column column="students.lastfirst">name</column>
			<column column="students.grade_level">grade_level</column>
 		</columns>
		<!--SQL query in format <![CDATA[QUERY]]>-->
        <sql>
			<![CDATA[
				SELECT
					student_number,
					lastfirst,
					grade_level
				FROM
					students
				WHERE
					enroll_status = 0
				ORDER BY lastfirst
			]]>
        </sql>
    </query>

</queries>